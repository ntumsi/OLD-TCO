Seed data is not included in this migration bundle.

Load lookup and data seed sets after running the schema/table migrations. Preserve identity values, release-version ranges, and effective-date columns during import.
