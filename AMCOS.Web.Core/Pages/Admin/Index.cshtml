@page
@{
    ViewData["Title"] = "Admin";
}

<div class="alert alert-secondary">
    Admin migration placeholder. Role-based navigation is active; admin feature parity still requires dedicated Razor Pages and controllers.
</div>
